import * as path from 'path';
import * as fs from 'fs';

import {
    Options
} from './options';

export async function getContractPaths(options: Options): Promise<string[]> {
    return [];
}

async function run(options: Options): Promise<any> {

}

export default run;
